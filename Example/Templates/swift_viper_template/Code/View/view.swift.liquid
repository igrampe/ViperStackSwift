{% include 'header' %}

import UIKit
import ViperStackSwift

class {{ prefix }}{{ module_info.name }}View: ModuleView, {{ prefix }}{{ module_info.name }}ViewInput {

    var output: {{ prefix }}{{ module_info.name }}ViewOutput?
}
